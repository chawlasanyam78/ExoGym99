@model IEnumerable<ExoGym.Models.membersdata>

@{
    ViewBag.Title = "Index";
}

<table class="table">
    <tr>
 
        <th>
            @Html.DisplayNameFor(model => model.username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.password)
        </th>
        <th></th>
    </tr>

    @foreach (var item in ViewBag.details)
    {
        <tr>
         
            <td>
                @item.username
            </td>
            <td>
                @item.password
            </td>
        </tr>     
    }

</table>
